{{#each this}}     
  <div class="row">
    <img src='{{Poster}}' alt='{{Title}}'>
    <button id="addMovie" type="button" class="btn btn-primary btn-lg">Add Movie To Your Wish List</button>
  </div>
{{/each}}