{{#each this}}
  <div class='col-xs-3 row-centered movie'>
    <a href='#'>
      <div class='thumbnail'>       
        <img src='{{Poster}}' alt='{{Title}}'>
        <button id="addMovie" type="button" class="btn btn-primary watched">Add</button>
      </div>
    </a>
  </div>
{{/each}}